{
  "name": "astro-framework-fixture",
  "private": true,
  "type": "module",
  "description": "Executable evidence for the `astro` entry in FRAMEWORK_PROFILES. Versions are pinned EXACTLY, not caret-ranged: every claim this fixture makes is version-sensitive compiler behaviour, and a caret would let a future install reproduce different evidence under the same commit.",
  "scripts": {
    "verify": "node verify.mjs",
    "mutate": "node mutate.mjs"
  },
  "devDependencies": {
    "astro": "5.14.1",
    "@astrojs/check": "0.9.4",
    "typescript": "5.6.3"
  }
}
