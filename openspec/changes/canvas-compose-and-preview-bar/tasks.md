## 1. Prerequisite: publish the foundation

- [x] 1.1 Synced (not archived — its 7.4 manual end-to-end pass is still open, so archiving would assert an unverified completion). `run-canvas`, `preview-inspector-bridge`, and `visual-token-editing` now exist under `openspec/specs/` with real Purposes; `openspec validate --all` 62/62.
- [x] 1.2 Fixed the `RunCanvas.tsx` docstring (documented a nonexistent "Pan" mode) and the `DesignPanel.tsx` comment (omitted Comment). Grep confirms no phantom-Pan references remain. Docs-only.

## 2. Unified canvas toolbar (canvas-toolbar)

- [x] 2.1 Extracted `CanvasToolbar` into `packages/ui/src/components/run-canvas/`: modes + zoom + bridge-status indicator, floating bottom-center, rendered by `RunCanvas` and outside the zoom transform so it doesn't scale with the stage. Exported as `@vortspec/ui/CanvasToolbar` so it can be driven directly in CT. **Insert is deliberately not on the bar yet** — `CanvasMode` has no `insert` member until 5.1, and the spec's "no phantom modes" requirement forbids naming an unreachable mode.
- [x] 2.2 Deleted the mode toggle from `DesignPanel`'s Layers header and the zoom footer; `mode`/`onModeChange`/`zoom`/`onZoomBy`/`onZoomReset` are gone from its props, along with the now-dead `ModeBtn`/`ZoomBtn` locals. **BREAKING (internal)** — `RunApp` updated; no other call site existed.
- [x] 2.3 Deleted the duplicate toggle in `CommentsPanel` and its `mode`/`onModeChange` props. Comment mode is operable from the toolbar in every state the toolbar allows it. **Known consequence, spec-sanctioned:** the toolbar is now the only entry to comment mode (`RunApp.tsx:834` picks the panel off `mode === "comment"`), so on a *failed* bridge — where the spec requires Comment disabled — the thread list is unreachable, even though reading/resolving threads needs no bridge (only pinning new ones does). Fixing 2.5's gate shrank this to genuine attach failures only, where anchors are meaningless anyway; it no longer fires on reloads. Reconsider if the always-reachable thread list is wanted — that is a spec change, not an implementation one.
- [x] 2.4 `RunCanvas` renders `CanvasToolbar`, wired to `mode`/`setMode` and the zoom state in `RunApp.tsx:301-304`. `interact` remains the default. Raised the bar to `z-40` so it clears the context menu's `z-30` dismiss backdrop, which otherwise ate the first click on it.
- [x] 2.5 Three states (`live` / `connecting` / `failed`) in `bridge-status.ts`. **Only `failed` disables Inspect/Comment** — the first cut gated on `!ready`, which `useInspectorBridge` clears on every `did-start-loading`, so the modes died on each agent-driven reload and swallowed clicks in the window. Interact never needs the bridge and is never disabled. The "visual editing unavailable" sentence now has one source (`bridgeStatusMessage`), reused verbatim by the notice and the toolbar's disabled reason. Spec prose said "not attached" while its scenario said "fails to attach" — that ambiguity licensed the bug; the requirement now says failed, and a "still connecting" scenario was added.
- [x] 2.6 CT: toolbar renders over the canvas with all modes + zoom; exactly one mode toggle and one zoom readout in the DOM; both survive collapsing Layers; `interact` is the resting default. Bridge states are covered three ways: connecting disables nothing (the reload-regression guard, via `<App />`), failed disables Inspect/Comment but not Interact and reads as a human sentence, live enables everything. The failed state is unreachable through `<App />` in CT — no `<webview>` means the guest never sends the `ok:false` ready event — so those two drive `CanvasToolbar` directly. 16/16 green in `apps/ide`.

## 3. ~~Canvas on the Playground~~ — CUT (design R1)

Cut wholesale. This section rested on a misreading of the activity keys: the activity *labelled* "Playground" is key `run`, and it **already has** the canvas, the three modes, and selection→chat. Key `play` is labelled "Storybook" and is the plain iframe. Nothing here needed doing, and extending the canvas to Storybook is a surface the user did not ask for — a separate change if ever. The `isApp` gate stays.

## 4. Ambient selection context (canvas-selection-context)

- [x] 4.1 Added `kind: "canvas-selection"` to `ChatAttachment` (`attachments.tsx`) with ICON/label/expansion, carrying label + prose and **no line range**. `PendingSelectionRef` gained a `source: "editor" | "canvas"` discriminant: the editor path keeps real lines; the canvas path (`App.tsx:538`) now sends `{ source: "canvas", label, text }` and the dock branches on it — the fabricated `startLine: 1, endLine: 1` is gone.
- [x] 4.2 Ambient via a small `CanvasSelectionProvider` (`packages/ui/src/lib/canvas-selection.tsx`), mirroring `assistant-task`. `RunApp` publishes the current selection (label + `buildSelectionContext` payload) in an effect keyed on `selection`/`pending`; the dock reads it with `useCanvasSelection` and renders a chip **without any gesture**. Re-select changes the key → chip replaces (never accumulates); a null selection (deselect / selection-lost, which the bridge already collapses to `readout=null`) withdraws it; and the canvas unmounting (leaving the Playground) publishes null via the cleanup effect.
- [x] 4.3 The ambient selection is **not** stored in the `attachments` array — it is derived from context and appended only at submit — so `setAttachments([])` can't consume it. It persists across turns for as long as the selection holds.
- [x] 4.4 Detachable + inspectable via a dedicated `CanvasSelectionChip`: the × sets a per-selection `detachedKey` (drops it for the next prompt; the canvas selection itself is untouched, and a re-select surfaces it again), and the chip expands to a `<pre>` showing exactly the `buildSelectionContext` payload that will be sent.
- [x] 4.5 Attaching never runs: publishing is a `setState`; the dock only folds the payload into the submit-time grounding string. It rides the `onSendToChat`/composer (attach) side and never touches `dispatchAssistantTask` (auto-run). CT asserts `__runPrompts` is empty after a bare select.
- [x] 4.6 Right-click "Send to chat" and the three canned actions still route through `onSendToChat` unchanged — but now as an honest `canvas-selection` attachment (no fake range) rather than a fabricated file reference. Still one-shot (cleared on submit), distinct from the ambient chip.
- [x] 4.7 CT (`apps/desktop/tests/ct/ambient-selection.ct.tsx`, 7 tests): chip appears on select with no gesture; grounds turn 1 and **survives** to ground the follow-up (both via `__runPrompts`); replaces on re-select; withdraws on deselect; detach sends without it (and a fresh select still grounds); a bare select starts no run. Driven through a `SelectionHarness` support component that stands in for the canvas — a `<webview>` guest, and thus a real bridge selection, doesn't exist in CT. **Not manually verified against a live dev server** — the `RunApp`→publish wiring is covered by typecheck, not an end-to-end select.

## 5. Insert-mode geometry — guest side (canvas-compose)

- [x] 5.1 `"insert"` added to `CanvasMode` (`useInspectorBridge.ts`) and the `setMode` protocol enum, and threaded through the guest, `RunCanvas`, and the toolbar (now carries Interact/Inspect/Comment/Insert; `NEEDS_BRIDGE` includes it, so a failed bridge disables it and Interact stays live). `RunApp` needed no change — it already passes `mode`/`onModeChange` through. Modes stay mutually exclusive; entering/leaving insert tears down its affordances on both sides.
- [x] 5.2 Extended the protocol (`inspector-bridge.ts`) with a shared `insertTarget` schema (anchor fingerprint + before/after + axis + line), the `createPlaceholder`/`resizePlaceholder`/`dismissPlaceholder` commands, and the `insertTarget`/`placeholderReady`/`placeholderLost` events. Round-trip tests added to `inspector-bridge.test.ts` (incl. bad-axis rejection and nullable target). `"insert"` intentionally NOT yet in the `setMode` enum — that flip belongs with 5.1 so the type stays consistent with the (still three-way) `CanvasMode`.
- [x] 5.3 `inferFlowAxis` in `packages/core/src/shared/insert-geometry.ts` — flex `flex-direction`, grid `grid-auto-flow`, else block→column. Pure, unit-tested. (Grid track-count is not consulted; auto-flow is sufficient to pick the axis, and clustering handles multi-track wrapping the same way it handles wrapped flex.)
- [x] 5.4 `resolveInsertTarget` + `visualRows` (same module): cluster into visual rows by cross-start proximity (wrapped flex → grouped by screen row, not DOM order), require a minimum cross-axis overlap before offering a pair's gap (nonsense cross-row gaps aren't offered), and apply slop. Every hit normalized to **anchor + before** (only a run's tail stays `after`). 20 unit tests cover gaps, wrap, nonsense-gap, slop, edges, empties.
- [x] 5.5 Midpoint fallback in `resolveInsertTarget`: past the element's midpoint → `after` (normalized to `before` the next sibling when one exists), before it → `before`. Unit-tested. **Corrected the spec scenario**, which read backwards ("`before` … if past its midpoint") — geometrically past-midpoint is nearer the end, i.e. `after`.
- [x] 5.6 The guest streams `insertTarget` as the pointer moves (throttled behind the existing rAF); `RunCanvas` draws an `InsertLine` — a thin bar, not a box — across the flow axis inside the scaled overlay, and sets the container cursor (`col-resize` for a vertical/row-flow line, `row-resize` for horizontal). Affordances render only when `mode === "insert"` (CT `interact-hides` asserts a stale target draws nothing outside insert mode).
- [x] 5.7 The guest materializes the placeholder on click (a `<div data-vs-overlay data-vs-placeholder pointer-events:none>` inserted at the slot), sized via `placeholderSizing(axis)` so a row insert fills its track rather than the parent's full width. Ephemeral — it carries `data-vs-overlay` so the tree scan and future hit-tests skip it, and nothing is written to disk.
- [x] 5.8 `PlaceholderBox` in `RunCanvas` draws e/s/se handles over the guest placeholder; dragging streams `resizePlaceholder` (client-px ÷ zoom → guest px). The n/w edges are omitted deliberately — they'd fight the fixed anchor edge. An explicit size overrides the implicit flex sizing as a **soft hint** the run may deviate from. (No live dev server was driven, so the *feel* of the resize is unverified — see 7.6.)
- [x] 5.9 HMR survival in `rebuildAndReacquire`: if the placeholder survived it re-echoes its rect; if a re-render dropped it, it re-acquires the anchor by fingerprint (reusing `resolveFingerprint`) and re-inserts. If the anchor is gone it dismisses and emits `placeholderLost` with a human sentence — never reattaches to the wrong element. The host surfaces that sentence as a dismissible notice.
- [x] 5.10 Two test layers: `insert-geometry.test.ts` (20 pure-geometry tests — gaps, wrapped rows, nonsense-gap, slop, midpoint, edges) and `insert-canvas.ct.tsx` (5 mock-bridge tests — vertical line for row flow, horizontal for column, placeholder box replaces the line, lost-notice, and affordances only in insert mode). **Not covered by an automated test:** the guest's actual DOM insertion + neighbour reflow + "writes nothing" — that runs only in a real `<webview>`, so it belongs to the 7.6 manual pass, not CT.

## 6. Roster-grounded composition run (canvas-compose)

- [x] 6.1 `buildComposePrompt` in `packages/core/src/shared/compose-run.ts` grounds on the roster (name/level/description/variants/props+options/tokens/file), the project tokens, DESIGN.md (capped), and the slot context — including the anchor's **leading text** as the disambiguator — plus the placeholder size hint. `rosterLine` formats each component. Pure + unit-tested.
- [x] 6.2 `DISTINCTNESS_CLAUSE` encodes Decision 8: read identity from real values first, each option a **different named axis**, prefer preserving identity, squint-test → **return fewer with a reason rather than pad**. The prompt states "at most N (1–N)" and "Never exceed 3". Unit-tested.
- [x] 6.3 `composeResultSchema` (same module): per-option `index`/`title`/`axis`/`componentsUsed`/`note`; top-level `fewerReason`; a `noMatch` (reason + suggestedName). Refinements enforce "options or noMatch", contiguous 0-based indices, and `.max(3)`. `parseComposeResult` extracts+validates the run's fenced JSON (the net-new agent-output parse pattern). Unit-tested.
- [x] 6.4 `hasUsableRoster(roster)` is the empty-roster signal (an absent/empty `.sdd-de/components.json` yields `components: []`). The prompt builder assumes a non-empty roster; the host gates on this and surfaces a next step (UI wiring lands with §6.5–6.11). Unit-tested.
- [x] 6.5 `ComposePanel` disables Generate until the prompt is non-empty, with a tooltip ("Describe what belongs here first"). `useComposeRun.generate` also refuses an empty intent. CT: gate asserted.
- [x] 6.6 One run in flight: `useComposeRun` is per-canvas (one `RunApp` per workspace) and `generate` early-returns while `phase === "generating"`; the panel replaces Generate with Cancel during a run, so a second can't be launched from the UI.
- [x] 6.7 `useComposeRun.generate` snapshots via `api.snapshotTokenScope` BEFORE the run, then `useAgentRun.start({ allowedTools: ["Read","Edit","Write"], bypassPermissions, strictMcp, model: sonnet })` — non-bare, argument-array spawn (inherited). CT asserts the composition prompt (with markers) ran.
- [x] 6.8 Host-side hard refusal added: `isCommittableSource` (git-adapter) rejects a git-ignored or untracked path (skipping the check outside a repo). `acceptComposition` refuses such a target as a last line (unit-tested against a real repo — untracked, ignored, and tracked cases), AND the host pre-checks via `compose:checkTarget` the moment the run reports its `writtenFile`, so the user is never offered accept into a generated/untracked file — they get a human sentence and Discard. CT covers the refusal.
- [x] 6.9 Escalated as a first-class result: the contract gained a `stopped` outcome (reason + candidates), the prompt instructs the run to return it — with candidates — on an ambiguous anchor and on a not-found anchor rather than guessing, and the hook surfaces it as a human sentence (naming the candidates) with Discard only. Because the run wrote nothing in that case, there is nothing to accept. Contract + parse unit-tested; the stop path CT-tested.
- [x] 6.10 Marker format + write instruction done (`compose-scaffold.ts` JSX-comment markers, inert in render; the prompt instructs the run to wrap each option). The disk write itself is performed by the §6.7 gated run. **Verified only through the mock run in CT — the real dev-server write is a 7.6 item.**
- [x] 6.11 Option cycler in `ComposePanel`: Prev/Next cycles options, each drives `bridge.previewOption(i)` (a new protocol command → guest injects a `<style>` hiding non-active `[data-vs-option]`); shows per-option title/axis/note and **provenance** (components used); surfaces `fewerReason` (never an empty slot); and a `no-match` state routes into extract-component. CT covers cycle + provenance + no-match→extract. **The in-place visual preview (the guest style filter) is not exercisable in CT — a 7.6 item.**
- [x] 6.12 `ComposePanel` shows the live run activity label and a Cancel while generating; `useComposeRun.cancel` cancels the run AND restores the snapshot.
- [x] 6.13 Accept keeps the chosen option and removes the rest (`composeAccept` IPC → `acceptComposition` = `stripScaffold(keepOption)`); Discard restores the snapshot (`restoreFiles`). Both go through `useComposeRun`. Accept is the only committed path. Main helper unit-tested (real files); the UI wiring CT-tested (accept records `keepOption`, discard records `restore`, no accept on discard).
- [x] 6.14 Idempotent, marker-driven cleanup, now with an automatic trigger: `sweepComposition` strips scaffold from named files, and `sweepProjectScaffold` (new `compose:sweepProject` IPC) uses `git grep --untracked` to FIND crash-orphaned scaffolds with no in-memory record of the file, then sweeps them. `RunApp` fires it on canvas open, so a scaffold left by a crash between write and accept is cleared next session. Discard/cancel also restore the snapshot on the happy path. All idempotent + unit-tested (tracked, untracked, and clean-project cases).
- [x] 6.15 Post-accept, `ComposePanel` shows a notice **naming the screen** (the written file) and offers "Update the screen spec" → routes through `dispatchAssistantTask` with a Screen-Creation-**update** prompt (update existing, not new). Non-blocking (Accept already committed the code). CT asserts the notice names the screen.
- [x] 6.16 `commit` in `git-adapter.ts` scans the staged files (`git diff --cached --name-only`) for the scaffold sentinel and refuses with a human sentence naming the live-preview files. **File-derived, not renderer state** — holds with no canvas mounted (real-git integration test, §7.3b). (The message tells the user to accept/discard; the terminal path is out of scope by design.)
- [x] 6.17 Count is 1–3, default 3, enforced in both directions: `buildComposePrompt` clamps the request into `[1,3]`, and `composeResultSchema` rejects >3. Unit-tested.

## 7. Tests & verification

- [x] 7.1 Vitest done: `insert-geometry.test.ts` (20 tests — axis inference, visual-row clustering, overlap threshold, slop, midpoint fallback, anchor normalization, placeholder sizing) and the insert bridge-schema round-trips in `inspector-bridge.test.ts`.
- [x] 7.2 Vitest done: `compose-run.test.ts` (18 tests) covers the prompt builder (roster + tokens + DESIGN.md + anchor leading-text + size hint + marker/JSON instructions + ambiguity/generated-file clauses) and the result contract (`no-component-match`, fewer-with-reason, >3 rejection, empty result rejection, non-contiguous rejection, empty-roster via `hasUsableRoster`), plus `compose-scaffold.test.ts` (8 tests) for the markers.
- [x] 7.3 CT via the mock API (`apps/ide/tests/ct/compose.ct.tsx`, 5 tests): empty roster refuses; Generate gated on intent; generating snapshots then cycles options with provenance and accepts (records `keepOption` on `__composeOps`, run prompt carries the markers); discard restores the snapshot and records no accept; a no-match routes into extract-component; accept surfaces the Screen Creation notice naming the screen. **Not asserted in CT** (guest-only, → 7.6): that accept leaves *zero markers in real source* and the in-place per-option preview — both need a real dev server. The marker-stripping itself is proven by `compose-apply.test.ts` against real files.
- [x] 7.3b Vitest done: `git-commit-guard.test.ts` runs real git in a temp repo — `commit` refuses a staged file carrying a scaffold marker (message names the file), then proceeds once it's removed, and allows a clean commit. No canvas/renderer state involved.
- [x] 7.4 **Crash-safety.** `compose-apply.test.ts` proves both the primitive and the trigger: `sweepProjectScaffold` finds a scaffold written into a file and then orphaned (as if the app died before accept) — with no in-memory record of the file — via `git grep`, strips it, and is a no-op on a clean project; it handles tracked and untracked orphans and leaves untouched files byte-identical. `RunApp` fires `composeSweepProject` on canvas open, so the recovery runs automatically. `stripScaffold`/`sweepComposition` are idempotent. **Still simulated, not a real killed process:** the test writes the partial scaffold directly rather than killing a live `claude` mid-write — that exact scenario is a 7.6 manual item, but the recovery path it would exercise is covered.
- [x] 7.5 Green across the board: `pnpm build` (4/4), `pnpm test` (all packages; 484 core unit tests incl. the new geometry/scaffold/compose/git-guard suites), `pnpm lint`, `pnpm check-types` (6/6, forced/uncached), and `openspec validate --all` (62/62). Playwright CT: ide 47/47, desktop 89/89.
- [ ] 7.6 Manual end-to-end through the running IDE against a real dev server (PRD "Done when", not just tests): pick a gap between two real elements, resize the placeholder, prompt it, cycle three roster-composed options, accept one, confirm it hot-reloads and that source contains exactly the accepted composition and no markers. Repeat the discard path and confirm the file is byte-identical to before.
