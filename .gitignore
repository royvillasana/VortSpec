node_modules
dist
.next
.turbo
*.tsbuildinfo

# env files
.env
.env.*
!.env.local.example

# OS
.DS_Store
Thumbs.db

# IDE
.idea
.vscode
*.swp

# Test snapshots can be committed
# but coverage output should not
coverage

# pnpm
pnpm-debug.log

# Playwright (component tests)
test-results
playwright-report
blob-report
**/playwright/.cache/

# electron-vite build output (regenerated by `build`/`dist`, never consumed from git)
apps/desktop/out/
apps/ide/out/

# electron-builder output (dmg/app — published as GitHub releases, never committed)
apps/desktop/release/
apps/ide/release/

# SDD-DE toolkit
.sdd-de/
packages/core/resources/vortspec-stamp.mjs
apps/ide/resources/vortspec-stamp.mjs
apps/desktop/resources/vortspec-stamp.mjs

# Screen recordings are the SOURCE for site/media/*.gif — keep them out of the repo
# (this one was 84MB). The published clips are the GIFs, which are committed.
*.mov
*.mp4
